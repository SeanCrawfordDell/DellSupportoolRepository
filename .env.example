# Git-native request tracking does not require browser credentials.
# Bug reports, feature requests, and catalog-status changes use GitHub Issue Forms.
#
# Never put a GitHub personal access token in a VITE_* variable: Vite exposes
# those values in the browser bundle. If API automation is added later, keep
# its credential in a server-side GitHub Action or serverless function.
